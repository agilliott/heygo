describe('Heygo frontend test site', () => {
  it('searches and navigates to a locations page', () => {
    cy.visit('/');
    // find text input
    // type for 'lon'
    // select first result
    // find button
    // click button
    // cy.url().should('include', '/locations/:id')
    // stuff should be on this page
    // find home button and click
    // expect to be home
  });
});
